# OPTIONAL — the app works without these (progress saves in your browser).
# Add them to upgrade to the real shared database (cross-device + pod leaderboard).
# Get them from your Supabase project: Settings -> API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
